<phone:PhoneApplicationPage 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:mvvm="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Extras.WP7"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800"
    x:Class="DMI.Views.SupportPage"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    DataContext="{Binding SupportViewModel, Source={StaticResource ViewModelLocator}}"
    SupportedOrientations="Portrait" 
    Orientation="Portrait"
    shell:SystemTray.IsVisible="True">
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <controls:Pivot Title="{Binding AppResources.AppTitle, Source={StaticResource AppResources}}">
            <controls:PivotItem Header="{Binding AppResources.SupportHeader, Source={StaticResource AppResources}}">
                <Grid>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding AppResources.AppName, Source={StaticResource AppResources}}" />
                            <TextBlock Text="{Binding AppResources.AppTitleSmall, Source={StaticResource AppResources}}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding AppResources.Version, Source={StaticResource AppResources}}" />
                            <TextBlock Text="{Binding Version}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding AppResources.CreatedBy, Source={StaticResource AppResources}}" />
                            <TextBlock Text="Claus Jørgensen" />
                        </StackPanel>
                        <TextBlock Margin="0,10,0,0" TextWrapping="Wrap" Text="{Binding AppResources.EmailHelpText, Source={StaticResource AppResources}}" />
                        <Button Content="{Binding AppResources.SendEmail, Source={StaticResource AppResources}}">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <mvvm:EventToCommand Command="{Binding OpenMailClient}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                        <TextBlock TextWrapping="Wrap" Text="{Binding AppResources.GPSHelpText, Source={StaticResource AppResources}}" />
                        <CheckBox IsChecked="{Binding AllowGPS, Mode=TwoWay}" Content="{Binding AppResources.AllowGPSUse, Source={StaticResource AppResources}}" />
                        <Button Content="ok">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <mvvm:EventToCommand Command="{Binding OK}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                    </StackPanel>
                </Grid>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>
</phone:PhoneApplicationPage>