<phone:PhoneApplicationPage 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:mvvm="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Extras.WP7"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800"
    x:Class="DMI.Views.SupportPage"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    DataContext="{Binding SupportViewModel, Source={StaticResource ViewModelLocator}}"
    SupportedOrientations="Portrait" 
    Orientation="Portrait"
    shell:SystemTray.IsVisible="True">
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <controls:Pivot Title="{Binding AppResources.AppTitle, Source={StaticResource AppResources}}">
            <controls:PivotItem Header="support">
                <Grid>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="AppName: " />
                            <TextBlock Text="{Binding AppResources.AppTitleSmall, Source={StaticResource AppResources}}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Version: " />
                            <TextBlock Text="{Binding Version}" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Created by: " />
                            <TextBlock Text="Claus Jørgensen" />
                        </StackPanel>
                        <TextBlock Margin="0,10,0,0" TextWrapping="Wrap" Text="Please write comments or technical questions to: email: 10229@iha.dk" />
                        <Button Content="send email">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <mvvm:EventToCommand Command="{Binding OpenMailClient}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                        <TextBlock TextWrapping="Wrap" Text="The location service (GPS) in DMI City Weather is sending your current location to a cloud service in order to convert the geographical coordinates into a postal code. The location is then used to display the weather-information for your area." />
                        <StackPanel Orientation="Horizontal">
                            <CheckBox IsChecked="{Binding AllowGPS, Mode=TwoWay}" />
                            <TextBlock Text="Allow use of GPS location" VerticalAlignment="Center" />
                        </StackPanel>
                        <Button Content="ok">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="Click">
                                    <mvvm:EventToCommand Command="{Binding OK}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </Button>
                    </StackPanel>
                </Grid>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>
</phone:PhoneApplicationPage>